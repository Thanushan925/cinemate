import 'package:flutter/material.dart';

class CinemaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cinema Page'),
      ),
      body: Center(
        child: Text('This is the cinema Page Content'),
      ), // Your content for the cinema/location page here.
    );
  }
}
